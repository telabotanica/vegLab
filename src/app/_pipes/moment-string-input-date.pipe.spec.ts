import { MomentStringInputDatePipe } from './moment-string-input-date.pipe';

describe('MomentStringInputDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MomentStringInputDatePipe();
    expect(pipe).toBeTruthy();
  });
});
