import { MomentLocalDatePipe } from './moment-local-date.pipe';

/*describe('MomentLocalDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MomentLocalDatePipe();
    expect(pipe).toBeTruthy();
  });
});*/
