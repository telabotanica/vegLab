import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PhytoHomePageComponent } from './phyto-home-page.component';

/*describe('PhytoHomePageComponent', () => {
  let component: PhytoHomePageComponent;
  let fixture: ComponentFixture<PhytoHomePageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PhytoHomePageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PhytoHomePageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
