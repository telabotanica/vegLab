import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PhytoAppPageComponent } from './phyto-app-page.component';

/*describe('PhytoAppPageComponent', () => {
  let component: PhytoAppPageComponent;
  let fixture: ComponentFixture<PhytoAppPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PhytoAppPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PhytoAppPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
