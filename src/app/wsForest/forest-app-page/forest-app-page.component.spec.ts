import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ForestAppPageComponent } from './forest-app-page.component';

/*describe('ForestAppPageComponent', () => {
  let component: ForestAppPageComponent;
  let fixture: ComponentFixture<ForestAppPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ForestAppPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ForestAppPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
