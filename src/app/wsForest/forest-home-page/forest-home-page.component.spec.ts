import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ForestHomePageComponent } from './forest-home-page.component';

/*describe('ForestHomePageComponent', () => {
  let component: ForestHomePageComponent;
  let fixture: ComponentFixture<ForestHomePageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ForestHomePageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ForestHomePageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
