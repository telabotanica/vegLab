import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminTestMetadataComponent } from './admin-test-metadata.component';

/*describe('AdminTestMetadataComponent', () => {
  let component: AdminTestMetadataComponent;
  let fixture: ComponentFixture<AdminTestMetadataComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdminTestMetadataComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminTestMetadataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
