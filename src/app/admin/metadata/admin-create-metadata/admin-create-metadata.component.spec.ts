import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminCreateMetadataComponent } from './admin-create-metadata.component';

/*describe('AdminCreateMetadataComponent', () => {
  let component: AdminCreateMetadataComponent;
  let fixture: ComponentFixture<AdminCreateMetadataComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdminCreateMetadataComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminCreateMetadataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
