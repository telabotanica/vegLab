import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminMetadataPageComponent } from './admin-metadata-page.component';

/*describe('AdminMetadataPageComponent', () => {
  let component: AdminMetadataPageComponent;
  let fixture: ComponentFixture<AdminMetadataPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdminMetadataPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminMetadataPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
