import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminRemoveMetadataComponent } from './admin-remove-metadata.component';

/*describe('AdminRemoveMetadataComponent', () => {
  let component: AdminRemoveMetadataComponent;
  let fixture: ComponentFixture<AdminRemoveMetadataComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdminRemoveMetadataComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminRemoveMetadataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
