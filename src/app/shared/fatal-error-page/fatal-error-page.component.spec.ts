import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FatalErrorPageComponent } from './fatal-error-page.component';

/*describe('FatalErrorPageComponent', () => {
  let component: FatalErrorPageComponent;
  let fixture: ComponentFixture<FatalErrorPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FatalErrorPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FatalErrorPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
