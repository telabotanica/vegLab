import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DrawSearchMapComponent } from './draw-search-map.component';

/*describe('DrawSearchMapComponent', () => {
  let component: DrawSearchMapComponent;
  let fixture: ComponentFixture<DrawSearchMapComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DrawSearchMapComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DrawSearchMapComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
